# Configuração da porta (necessário para Render)
server.port=${PORT:8081}

# Conexão com o banco Oracle
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
spring.datasource.username=rm557937
spring.datasource.password=120304
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Configuração do Hibernate / JPA
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update

# Exibir SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Forçar encoding UTF-8 para Maven/Recursos
spring.datasource.hikari.connection-init-sql=SET NLS_LANG=AMERICAN_AMERICA.AL32UTF8
